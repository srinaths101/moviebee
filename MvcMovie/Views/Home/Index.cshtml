@model Tuple<IEnumerable<TMDBMovie>, IEnumerable<TMDBMovie>, IEnumerable<TMDBMovie>, IEnumerable<TMDBMovie>>
@{
    ViewData["Title"] = "Home Page";
}

   <div>
                <h1 style="margin-top:10%;margin-bottom: 5%">Movie Bee let's you:</h1>
                <div class="row">
                    <div class="column" style="background-color:#aaa;">
		            <img src="~/images/download.png" width="225" height="225" alt=""/>
                    <p>Review Your Favourite movies with a like</p>
                    </div>
                    <div class="column" style="background-color:#bbb;">
		            <img src="images/share_android-512.jpg" width="225" height="225" alt=""/>
                    <p> Write And Share Reviews</p>
                    </div>
                </div>

                <div class="row">
                    <div class="column" style="background-color:#ccc;">
                    <img src="images/download1.jpg" width="225" height="225" alt=""/>
                    <p>Rate Each Film on a five-star scale</p>
                    </div>
                    <div class="column" style="background-color:#ddd;">
                    <img src="images/recommended-icon-line-label-recommended-vector-28138175.jpg" width="225" height="225" alt=""/>
                    <p>Get List of Recommended movies For you</p>
                    </div>
                </div>
            </div>
    </div>

@if (ViewData["isError"] == "false")
{
    <div>
        <section class="product-images">
            @if (ViewData["isRecommended"] == "true")
            {
                <div class="containers-feature">

                    <h1 style="margin-top: 7%;margin-bottom: 7%;">Recommended Movies for you</h1>
                    @foreach (var item in Model.Item4)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }
                </div>
                <div class="containers-feature">
                    <h1 style="margin-top: 30%;margin-bottom: 7%;">Top Rated Movies</h1>
                    @foreach (var item in Model.Item1)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div>
                <div class="containers-feature">
                    <h1 style="margin-top: 30%;margin-bottom: 7%;">Popular Movies</h1>
                    @foreach (var item in Model.Item2)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div>
                <div class="containers-feature">
                    <h1 style="margin-top: 30%;margin-bottom: 7%;">Upcoming Movies</h1>
                    @foreach (var item in Model.Item3)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div>
            }
            else
            {
                @* <div class="containers-feature">
                    <h1 style="margin-top: 7%;margin-bottom: 7%;">Top Rated Movies</h1>
                    @foreach (var item in Model.Item1)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div> *@
                @* <div class="containers-feature">
                    <h1 style="margin-top: 30%;margin-bottom: 7%;">Popular Movies</h1>
                    @foreach (var item in Model.Item2)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div> *@
                @* <div class="containers-feature">
                    <h1 style="margin-top: 30%;margin-bottom: 7%;">Upcoming Movies</h1>
                    @foreach (var item in Model.Item3)
                    {
                        <section class="fatured" style="margin-bottom: 30px;">
                            <a asp-controller="Movies" asp-action="TMDBDetails" asp-route-id="@item.id"><img class="product-img"
                                    src="@Url.Content(String.Format("{0}{1}", ViewData["baseImageUrl"], item.poster_path))"
                                    alt="@item.title" /></a>
                        </section>
                    }

                </div> *@
            }
            </div>
         
}
else
{
    <h3 style="margin-top: 9%;margin-bottom: 7%;">Problem Occured while fetching data.</h3>
}
